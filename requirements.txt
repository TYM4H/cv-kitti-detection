# На Kaggle устанавливать только это (torch/torchvision системные, не трогать):
#   pip install ultralytics torchmetrics -q
#
# Локально (CPU-среда для разработки):
ultralytics>=8.2.0
torchmetrics>=1.4.0
Pillow>=10.0.0
matplotlib>=3.8.0
numpy>=1.26.0
tqdm>=4.66.0
pyyaml>=6.0.1
